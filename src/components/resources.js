import React from 'react';

import Header from './header';
import UserList from './user-list';

export default (props) => {

    return (
        <div>
            <Header />

            <UserList />

        </div>
    );
}